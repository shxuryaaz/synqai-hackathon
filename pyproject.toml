[project]
name = "meridian-ops"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["openpyxl>=3.1", "pyyaml>=6", "fastapi>=0.115", "uvicorn>=0.30", "python-dotenv>=1", "openai>=1.40", "python-multipart>=0.0.9"]
[dependency-groups]
dev = ["pytest>=8"]
